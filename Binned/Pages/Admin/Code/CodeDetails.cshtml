@page
@model Binned.Pages.Admin.Code.CodeDetailsModel
@{
}
<form class="mt-3" method="post">
    @*<span asp-for="@Model.Codeid" name="id">@Model.Codeid</span>*@
    @*<div class="row mb-3 h3">#@Model.Codeid</div>*@
    <div class="row mb-3">Code Id: @Model.Code.Id</div>

    <div class="row mb-3">Date Created: @Model.Code.CreateDate.ToShortDateString()</div>

    <div class="row mb-3"> Date: @Model.Code.ExpiryDate.ToShortDateString()</div>

    <div class="row mb-3">Status: @Model.Code.Active</div>

    <div class="row mb-3">Discount: @Model.Code.Discount</div>

    @*    <div class="row mb-3">

    <label class="ps-0 col-2 col-form-label" asp-for="status">Update status</label>
    <div class="col-10">
    @Html.DropDownListFor(m => m.status, new SelectList(Status.Statuses, "Value", "Name"), "-- Select --", new {@class = "form-select"})
    <span class="text-danger"></span>
    </div>
    </div>*@
    <div>
        <input type="submit" value="Update" class="btn btn-primary" />
        <a class="btn btn-secondary ms-2" href="/Admin/Code/PromoCode">Cancel</a>
    </div>
</form>
